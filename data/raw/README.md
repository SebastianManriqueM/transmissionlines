# Raw reference data

`Tower_geometries_DB.xlsx` is preserved unchanged from the Julia reference
repository `SebastianManriqueM/TL_parameter_computation`. It is the source
artifact for the v3 catalog and is not read by calculation modules.

Regenerate the normalized catalog with:

```powershell
python scripts/build_reference_catalog.py
```
