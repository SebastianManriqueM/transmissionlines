Command-line interface
======================

The project installs a ``tl`` console entry point, but the current application
is deliberately a placeholder. It does not parse arguments or implement
catalog, model-building, or calculation subcommands yet.

.. automodule:: transmissionlines.cli.app
   :members: